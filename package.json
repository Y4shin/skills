{
  "name": "prd-workflow",
  "version": "0.11.0",
  "description": "PRD workflow: epic → PRD → slices → TDD → finalize. Pi extension + opencode plugin.",
  "keywords": ["pi-package"],
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "smol-toml": "^1.3.1",
    "yaml": "^2.6.1"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.80.3",
    "@opencode-ai/plugin": "^1.17.4",
    "@types/node": "^20.11.0",
    "esbuild": "^0.24.0",
    "typebox": "^1.3.4",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "pi": {
    "extensions": ["./src/pi/index.ts"],
    "skills": ["./skills"]
  }
}