{
  "name": "prd-tool",
  "version": "0.10.3",
  "description": "Query & mutate the YAML frontmatter of a repo's docs/prd planning tree (prd-workflow plugin) — single TypeScript implementation shared by the Claude Code and opencode plugins.",
  "type": "module",
  "bin": {
    "prd-tool": "dist/prd-tool.js"
  },
  "scripts": {
    "build": "node build.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "smol-toml": "^1.3.1",
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.17.4",
    "@types/node": "^20.11.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
