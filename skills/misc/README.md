# Misc

Tools kept around but rarely used; **not promoted** (not in `package.json`
`pi.skills`, no docs page, not shipped). Install directly if needed.

_(empty for now; the `adopt-mp-skills-way` map adopts none of Matt's misc
skills.)_
