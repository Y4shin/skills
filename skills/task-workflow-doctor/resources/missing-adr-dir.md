# Missing `docs/adr/` directory

## Symptom

- Architecture Decision Records have no home.
- ADRs are expected under `docs/adr/` but the directory is missing.

## Missing artifact

`docs/adr/` directory.

## Route

The relevant skill creates `docs/adr/` lazily when adopted. Until then, create the directory manually, or adopt the relevant skill.
