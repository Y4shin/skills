# Productivity

General workflow tools, not code-specific. **Promoted**: shipped (in
`package.json` `pi.skills`) and each gets a human-facing docs page at
`docs/productivity/<name>.md`.

## User-invoked

Reachable only when you type them (`disable-model-invocation: true`).

- **[wait-what](./wait-what/SKILL.md)**: Stop. That last message did not land:
  re-pitch it.

## Model-invoked

Model- or user-reachable.

_(none yet; `grill-me`, `handoff`, `to-questionnaire`, `teach`, and
`writing-for-agents` are added by the `adopt-mp-skills-way` map.)_
