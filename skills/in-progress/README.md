# In Progress

Beta. Public on purpose: try them and tell us what breaks. **Not promoted**
(not in `package.json` `pi.skills`, no docs page, not shipped); can change or
disappear without warning.

_(empty for now; the `adopt-mp-skills-way` map adopts none of Matt's
in-progress skills.)_
